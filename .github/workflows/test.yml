name: samplePipeline
on:
  workflow_dispatch:
jobs:
  Hello:
    runs-on: self-hosted
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: echo message
      run: echo Hello World
